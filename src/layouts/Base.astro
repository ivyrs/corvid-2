---
import "~/styles/global.css";
import "~/styles/omg.css";

import Metadata from "~/components/util/Metadata.astro";
import Header from "~/components/ui/Header.astro";
import Footer from "~/components/ui/Footer.astro";

const { title, desc, showHeader = true, showFooter = true } = Astro.props;
---

<!doctype html>
<html lang="en">
	<Metadata title={title} desc={desc} />

	<body
		class="bg-yang text-yin dark:bg-yin dark:text-yang p-4 sm:p-8 md:px-16 text-lg text-pretty"
	>
		<a href="#content" class="hidden selection:inline">skip to content</a>
		{showHeader && <Header />}

		<slot />

		{showFooter && <Footer />}
	</body>
</html>
