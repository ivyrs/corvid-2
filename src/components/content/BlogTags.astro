---
const { tags } = Astro.props;
---

<div class="flex gap-2 mt-2 text-sm">
  {
    tags.map((i: string[]) => {
      return (
        <span class="text-red-7 dark:text-pink-4">
          <a href={"/blog/tags/" + i}>#{i}</a>
        </span>
      );
    })
  }
</div>
