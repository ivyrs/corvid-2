---
import Block from "./Block.astro";
import CDate from "~/components/ui/Date.astro";
import { getNowPosts } from "~/lib/now";
import { sectionThemes } from "~/lib/sectionThemes";

const posts = (await getNowPosts()).slice(0, 1);
---

<Block title="how i'm feeling /now" theme={sectionThemes.now} links={[{href: "/now"}]}>
	<div class="space-y-4">
		{posts.map((post) => (
			<article>
				<div class="mb-1 text-yin-7 dark:text-yin-3">
					<CDate date={post.id} />
				</div>

				<p class="line-clamp-3">
					{post.body}
				</p>
			</article>
		))}
	</div>
</Block>
