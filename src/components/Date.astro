---
import { formattedDate, isoDate } from "~/lib/time";
interface Props {
  date: Date | string;
  showTime?: boolean;
}
const { date, showTime } = Astro.props;
---

<time datetime={isoDate(date)}>{formattedDate(date, showTime)}</time>
